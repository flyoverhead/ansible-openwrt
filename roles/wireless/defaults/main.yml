---
# Every variable below is empty by default, so the role is a no-op until it is
# configured. The accepted keys for each entry are documented in README.md.

# device section
wireless_devices: []

# interface section
wireless_interfaces: []
