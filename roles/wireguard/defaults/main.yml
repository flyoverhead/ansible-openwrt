---
# Wireguard packages
wireguard_pkgs:
  [wireguard-tools, luci-proto-wireguard, luci-app-wireguard, qrencode]

# The two below are empty by default, so the role only installs packages until
# it is configured. The accepted keys for each entry are documented in README.md.

# Wireguard interfaces
wireguard_interfaces: []

# Wireguard peers
wireguard_peers: []
