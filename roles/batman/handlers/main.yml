---
# Reload wireless service
# `wifi config` regenerates /etc/config/wireless from the committed UCI state,
# `wifi reload` brings the radios back up with it
- name: Reload wireless
  ansible.builtin.command:
    cmd: /sbin/wifi {{ item }}
  changed_when: true
  loop: [config, reload]
