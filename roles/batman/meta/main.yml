---

galaxy_info:
  role_name: batman
  namespace: flyoverhead
  author: flyoverhead
  description: B.A.T.M.A.N. adv mesh network interfaces and mesh-capable wpad
  license: GPL-3.0-only
  min_ansible_version: '2.16'
  platforms:
    - name: OpenWrt
      versions:
        - '22.03'
        - '23.05'
  galaxy_tags:
    - batman
    - mesh
    - networking
    - openwrt
    - wireless

# Provides the `uci` and `opkg` modules and the action-plugin monkeypatch that
# rewrites `ansible.builtin.*` to shell equivalents. Resolved from `roles/`
# through `roles_path`; see requirements.yml.
dependencies:
  - role: gekmihesg.openwrt
